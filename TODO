- Make sure all required fields in POST requests (in all controllers) actually exist
- Handle HTTP codes correctly in controllers
- Make TokensController::loginUser() async? (use async version of jwt.sign())
- Send password hash on login
- Do not sign password in jwt
- Use a real, unique, non-published private key to sign tokens
- Use the DB for user management
- Make input fields valid only if trimmed string is valid
- Use 'express-jwt'
    - import * as jwt from 'express-jwt'
    - @UseBefore(jwt({srcret: ''}))
- Use LocationStrategyHash
- AuthGuard: CanActivate
    - To protect full routes at client-side
- Unify conventions
    - Number of spaces in a tab